#!/bin/bash

echo "开始构建测试..."
echo "================================"

# 运行构建命令
npm run build

# 检查构建结果
if [ $? -eq 0 ]; then
    echo "================================"
    echo "✅ 构建成功！所有 TypeScript 错误已修复。"
else
    echo "================================" 
    echo "❌ 构建失败，仍有错误需要修复。"
    exit 1
fi
